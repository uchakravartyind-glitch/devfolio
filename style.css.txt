/* Colour & Type System */
:root {
  --brand: #2563eb;
  --bg-color: #f8fafc;
  --card-bg: #ffffff;
  --text-main: #1e293b;
  --border-color: #e2e8f0;
}

body {
  margin: 0;
  font-family: system-ui, -apple-system, sans-serif;
  line-height: 1.6;
  background-color: var(--bg-color);
  color: var(--text-main);
}

/* Flexible images safeguard */
img {
  max-width: 100%;
  height: auto;
}

/* Header & Flexbox Nav */
header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem 2rem;
  background-color: var(--card-bg);
  border-bottom: 1rem solid var(--border-color);
}

nav a {
  margin-left: 1rem;
  color: var(--brand);
  text-decoration: none;
  font-weight: 500;
}

main {
  max-width: 900px;
  margin: 0 auto;
  padding: 2rem 1rem;
}

section {
  margin-bottom: 3rem;
}

/* Mobile-First Projects CSS Grid (1 Column) */
.projects-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1.5rem;
}

.project-card {
  background-color: var(--card-bg);
  padding: 1.5rem;
  border-radius: 8px;
  border: 1px solid var(--border-color);
}

.project-card a {
  color: var(--brand);
  text-decoration: none;
  font-weight: bold;
}

footer {
  text-align: center;
  padding: 2rem;
  background-color: var(--card-bg);
  border-top: 1px solid var(--border-color);
}

footer a {
  color: var(--brand);
}

/* Desktop Responsive Breakpoint (2 Columns) */
@media (min-width: 600px) {
  .projects-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}